apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: mongo
  labels:
    app: mongo
spec:
  clusterIP: None # headless
  selector:
    app: mongo
  ports:
    - port: 27017
      name: mongo